<Window x:Class="IHM.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="IHM Principale" MinHeight="600" MinWidth="600"
        Height="auto" Width="auto"
        Loaded="Window_Loaded" MouseLeftButtonDown="Window_MouseLeftButtonDown">
    
    <DockPanel x:Name="panel">

        <StackPanel DockPanel.Dock="Top">
            <Grid VerticalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"></RowDefinition>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                    <ColumnDefinition Width="4*"></ColumnDefinition>
                    <ColumnDefinition Width="2*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                
                <StackPanel Orientation="Vertical" Grid.Column="0" Grid.Row="0">
                    <Label x:Name="Nation1Label" Content="Peuple : " />
                    <Label x:Name="Units1Label" Content="Unité restantes : "/>
                </StackPanel>

                <StackPanel Orientation="Vertical" Grid.Column="1" Grid.Row="0">
                    <Label x:Name="Nation2Label" Content="Peuple : " />
                    <Label x:Name="Units2Label" Content="Unité restantes : "/>
                </StackPanel>

                <Button HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" Content="Fin du tour" Click="EndOfTurnButton_Click" Grid.Column="2" Grid.Row="0"/>

                <!--<StackPanel DockPanel.Dock="Bottom">
            <Grid VerticalAlignment="Top">
                <StackPanel Orientation="Vertical" HorizontalAlignment="Left">
                    <Label x:Name="labelNation1" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                </StackPanel>
                <StackPanel Orientation="Vertical" VerticalAlignment="Top" HorizontalAlignment="Right">
                            <Label x:Name="labelNation2" VerticalAlignment="Top" HorizontalAlignment="Center"/>
                        </StackPanel>

                <Button HorizontalAlignment="Center" VerticalAlignment="Center" Height="30" Content="Fin du tour" Click="EndOfTurnButton_Click" Grid.Column="2" Grid.Row="0"/>
            -->
            </Grid>
        </StackPanel>
        
		
        <StackPanel x:Name="unitInfoPanel" VerticalAlignment="Center" Orientation="Horizontal" DockPanel.Dock="Bottom" />

        <!--   <Grid VerticalAlignment="Bottom">
                <StackPanel Orientation="Horizontal">
                    <Label Name="InfoLabel"/>
                    <Label Name="MapLabel"/>
                </StackPanel>
                <Button HorizontalAlignment="Right" Content="Fin du tour"  />
            </Grid>
		-->
		
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom">
            <Label Name="MapLabel"/>
            <Label Name="InfoLabel"/>    
        </StackPanel>
        
        <ScrollViewer HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto" DockPanel.Dock="Top">
            <Grid Name="mapGrid" Background="Orange" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        </ScrollViewer>
    
    </DockPanel>
</Window>
